/*
 * This file was generated by the Gradle 'init' task.
 *
 * This generated file contains a sample Java project to get you started.
 * For more details take a look at the Java Quickstart chapter in the Gradle
 * User Manual available at https://docs.gradle.org/5.6.3/userguide/tutorial_java_projects.html
 */
plugins {
	    // Apply the java plugin to add support for Java
	    id 'java-library'
}

apply from: 'config.gradle'

allprojects {
    group = 'com.sele'
    apply plugin: 'java'
    //apply plugin: 'org.springframework.boot'
    //apply plugin: 'io.spring.dependency-management' 
    apply plugin: 'maven'
    //apply plugin: "idea"    
    //apply from: "${rootProject.projectDir}/config.gradle"

    sourceCompatibility = 1.8
    targetCompatibility = 1.8

    tasks.withType(JavaCompile) {
        options.encoding = 'UTF-8'
    }
    
    repositories {
        maven { url 'http://maven.aliyun.com/nexus/content/groups/public/'}
        mavenCentral()
        jcenter()
        //maven { url "https://jitpack.io" }
        //maven { url "https://maven.google.com" }
        google()
    }

    //配置依赖
    //dependencies {
        //compile(
               // "org.springframework:spring-webmvc:${rootProject.ext.springVersion}",
                //"org.springframework:spring-jdbc:${rootProject.ext.springVersion}",
                //"org.springframework:spring-web:${rootProject.ext.springVersion}",
                //"org.springframework:spring-oxm:${rootProject.ext.springVersion}",
                //"org.springframework:spring-context-support:${rootProject.ext.springVersion}",
               // "com.mangofactory:swagger-springmvc:1.0.2"
                //"com.fasterxml.jackson.core:jackson-annotations:${rootProject.ext.jacksonVersion}",
                //"com.fasterxml.jackson.core:jackson-databind:${rootProject.ext.jacksonVersion}",
                //"com.fasterxml.jackson.core:jackson-core:${rootProject.ext.jacksonVersion}"
                //"org.springframework.boot:${springbootVersion}"
        //)
        //testCompile("org.testng:testng:${rootProject.ext.testngVersion}")
    //}
    
    test {
    // Use TestNG for unit tests
    useTestNG()
	}
    
}

dependencies {
    // This dependency is used by the application.
    implementation 'com.google.guava:guava:28.0-jre'

    // Use TestNG framework, also requires calling test.useTestNG() below
    //testImplementation 'org.testng:testng:6.14.3'
}
